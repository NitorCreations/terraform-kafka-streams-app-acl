# Kafka ACL for Kafka Streams App

This module uses [`terraform-provider-kafka`](https://github.com/Mongey/terraform-provider-kafka) and defines a set of ACL entries required to run a Kafka Streams application.
